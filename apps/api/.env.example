# Copy this file to .env (which is in .gitignore) and fill in the real secret locally.
# Do NOT commit your real credentials. The project .gitignore already excludes .env files.
# Example format for a Neon / PostgreSQL connection string:
# postgres://<user>:<password>@<host>:<port>/<database>?sslmode=require&channel_binding=require

DATABASE_URL="postgresql://neondb_owner:REPLACE_ME@ep-fragrant-shape-adeba20e-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Optional: set NODE_ENV for local testing
# NODE_ENV=development
